<%= form_for(@position) do |f| %>
  <% if @position.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@position.errors.count, "error") %> prohibited this position from being saved:</h2>

      <ul>
      <% @position.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= collection_select(:position, :area_id, Area.all, :id, :name, 
       {prompt: 'Selecione uma area'}, {class: 'chosen-select'}) %>
  </div>

  
  <div class="actions">
    <%= f.submit %>
  </div>
    
<% end %>

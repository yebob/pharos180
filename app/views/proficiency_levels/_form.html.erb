<%= form_for(@proficiency_level) do |f| %>
  <% if @proficiency_level.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@proficiency_level.errors.count, "error") %> prohibited this proficiency_level from being saved:</h2>

      <ul>
      <% @proficiency_level.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  Level
  <%= collection_select(:proficiency_level, :level_id, Level.all, :id, :name) %> <br>
  Position
  <%= collection_select(:proficiency_level, :position_id, Position.all, :id, :name) %> <br>
  Skill
  <%= collection_select(:proficiency_level, :skill_id, Skill.all, :id, :name) %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

<%= form_for(@skill_cat) do |f| %>
  <% if @skill_cat.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@skill_cat.errors.count, "error") %> prohibited this skill_cat from being saved:</h2>

      <ul>
      <% @skill_cat.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

<%= form_for(@skill_relevance) do |f| %>
  <% if @skill_relevance.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@skill_relevance.errors.count, "error") %> prohibited this skill_relevance from being saved:</h2>

      <ul>
      <% @skill_relevance.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

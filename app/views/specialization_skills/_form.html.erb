<%= form_for(@specialization_skill) do |f| %>
  <% if @specialization_skill.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@specialization_skill.errors.count, "error") %> prohibited this specialization_skill from being saved:</h2>

      <ul>
      <% @specialization_skill.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
